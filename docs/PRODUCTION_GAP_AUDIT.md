# SYNAPSE-S2 Production Gap Audit

This file is intentionally blunt. It catalogs prototype-risk gaps, shorthand fixes, and current disposition so operators do not mistake demo scaffolding or research extensions for production guarantees.

Deployment status is also intentionally separate from code disposition. No row
below claims remote publication, live cutover, bridge governance, or multi-Mac
replication; determine those states through their explicit runtime, Git, and
replication evidence contracts.

## Closed In Latest Hardening Pass

| Gap | Risk | Shorthand solution | Disposition |
| :--- | :--- | :--- | :--- |
| The durable authority-lock generation used macOS `st_dev`, which can change across reboot even when the owner-only lock file is unchanged | A healthy v6 store could become correctly fenced but unrestartable after reboot; blindly adopting the new device number would repeat the failure and weaken replacement detection | Use stable macOS inode-plus-birth-time `lockfs-v2`, retain live held-versus-visible device/inode checks, and permit v1→v2 only inside a signed recovery-backed build replacement | Fixed in `core_authority.py`, `core_service.py`, `memory_store.py`, recovery validation, and the installer. Replacement-admission v3 signs predecessor and candidate generations, transition mode, and birth time. Same-inode legacy migration requires a private zero-byte lock predating the durable claim plus fresh paired recovery/isolated restore; changed inode, new birth time, arbitrary v2 drift, ordinary recovery, and unsigned claim remain blocked. Stopped status now distinguishes guarded migration from build drift and unsafe lock mismatch without creating SQLite sidecars. |
| Persistent MLX arrays were first evaluated by a different RPC worker than the startup thread that created them | Lazy arrays retained the constructor thread's default GPU stream and could emit `There is no Stream(gpu, 0) in current thread`, degrading or failing ordinary neural work after apparently healthy startup | Materialize every persistent neural array inside the startup execution context before worker handoff, then keep the existing per-worker stream context | Fixed in `mlx_backend.py`; startup now fails before authority publication if the eager materialization barrier fails, and a real-MLX constructor/worker regression proves durable trace registration without the stream warning. |
| Invalid `test-validated` Cortex evidence reached the mutation journal before deterministic contract rejection | A correctable field-name error could become an ambiguous request that must never be blindly replayed | Share one pure evidence contract between protocol admission and backend validation, normalize truth posture, and reject deterministic no-effect failures before journal acceptance | Fixed in `cortex_contract.py`, `core_service.py`, and `mlx_backend.py`; tests prove invalid evidence consumes no journal row while recognized test/output/artifact evidence remains supported. |
| Dashboard Doctor launched a global semantic audit before completing ordinary checks | The audit owned the serialized backend lane, Doctor then waited behind its own work, and the browser looked frozen even though the core was in bounded maintenance | Keep daily Doctor non-launching and quick, expose an explicit deep-scan action, add a lock-free read-only core-health pulse, and retain the last good namespace map during maintenance or bounded read failure | Fixed in `dashboard_server.py` and `web/*`; the UI reports `READY`, `MAINTENANCE`, or `OFFLINE`, shows the lane owner and timing in the runtime label, bounds GET reads, and refreshes health every five visible seconds without aborting mutations. |
| Symmetric namespace Dice could bury a focused namespace beside a much denser related namespace | A small project such as PTZPLZ could fail to surface a relevant large CASP namespace despite several meaningful shared signals | Preserve density-normalized Dice as the baseline, require semantic surface corroboration, and apply a conservative sparse-to-dense containment lift only after multiple surface overlaps | Fixed in `memory_store.py`; suggestions expose both baseline and lifted evidence, remain read-only, require explicit revision-bound governance, and sparse/dense regressions reject both a one-term collision and spike-space saturation without shared surface evidence. |
| Agent hydration reported internal graph relationships without an explicit namespace-bridge capsule | Operators and agents could mistake six relationships inside PTZPLZ for six governed links to other contexts | Add a bounded connectivity capsule that states local scope, active and pending bridges, connected namespace ids, no automatic cross-write, and no live multi-Mac sync | Fixed in `mlx_backend.py` and `token_contracts.py`; compact hydration and rendered briefing explicitly distinguish internal relationships from namespace bridges. |
| CLI, MCP, dashboard, and capture processes could each construct a full backend and disagree about runtime state | Duplicate neural substrates, stale writers, split toggles, and capture races could look healthy while durable state diverged | Move all mutable/backend work behind one private authenticated Unix-socket core, publish a durable v6 authority epoch, fence stale/forked/replaced-lock writers, embed capture, and make every adapter fail closed without fallback | Fixed by the authoritative core, full closed-marker digest fence, stable lock/database generations, runtime-state v3 epoch binding, bounded request workers and teardown, request journal, routed adapters, installer, and cutover controls. Lock-generation rollover is restored-target-only; a stalled teardown retains authority until process termination. The dashboard plist contains no DB/state/neural settings, and the legacy capture installer refuses v6 or an installed core before mutation. |
| A failed first adoption could leave an empty request journal that blocked every retry | Blind deletion could erase the only durable evidence of an accepted mutation; opening canonical WAL residue during inspection could consume or change that evidence | Permit repair only under an exact unbound v5 core lease; prove the full memory schema/identity before and after; seal journal artifacts; replay WAL only in an isolated copy; require exact schema/store binding and zero rows; archive through crash receipts | Fixed in `core_request_journal.py`, direct `core_service.py` startup, and installer preflight. Rollback/unknown transients, SHM without WAL, malformed or extra schema, one or more request rows, identity drift, orphan archives, and unverifiable temp/scratch files fail closed. Pending/complete/retiring receipts resume partial renames and retention; at most eight verified repair sets are kept, with oldest-first crash-resumable retirement. The service and installer call the same primitive around the same exact pre/post `DurableMemoryStore` inspection. |
| The durable v6 marker could commit before its matching runtime-state JSON was published | A power loss in the cross-store window could strand a correct marker behind a missing or stale runtime file, while broad repair could legitimize unrelated drift | Commit a marker-bound pending publication receipt in SQLite, atomically publish and reread runtime state, complete the receipt, and permit only same-lock-generation recovery of the exact pending identity | Fixed in `memory_store.py`, `mlx_backend.py`, and `core_service.py`. Recovery requires an unbound successor lease on the same visible lock generation plus exact marker, epoch, instance, config, build, protocol, root, embedding-space, and runtime-path identities. Changed identity, replaced lock/root, malformed receipt, or an already-complete publication paired with stale state still fails closed. No cross-filesystem atomicity is claimed. |
| A reviewed non-default MLX device could disappear when launchd started the core | The certifier and installer could review `cpu` or `gpu` while production silently ran `default`, invalidating effective-config and performance evidence | Close the full CoreConfig, verify it matches the reviewed layout, and publish its exact `mlx_device` into the LaunchAgent environment | Fixed in `scripts/core_agent_installer.py`; the plist contains the non-secret build ID and exact closed `MLX_DEVICE`, while token, model, topology, database, state, and capture settings remain out of the environment. Covered by default and non-default installer/runtime tests. |
| Slow or stalled local connections and teardown hooks could exhaust service resources or release authority too early | A same-UID slowloris could consume workers, or a hung close could overlap writer epochs | Bound backlog/workers/frame and socket timeouts; close admission first; serialize backend work; use bounded shutdown drains; retain and poison authority on non-quiescence | Fixed in `core_service.py`: backlog 64, at most 32 active core workers, one-second pre-authentication frame/peer/authentication window, five-second authenticated socket I/O timeout, and separate bounded teardown/worker drains. A stalled backend lane or failed close retains authority-bearing references until process termination instead of reporting clean shutdown. The dashboard has its own stricter eight-handler/backlog-32 bounds, absolute one-second header deadline, five-second post-header I/O, and bounded shutdown. |
| A transient pre-connect launchd or socket-backlog race could fail a mutation-class client operation without one safe retry | Start Work and other compound operator flows could report `service_unavailable` even though no request bytes ever reached the authoritative core | Retry the same signed request id once only after connection establishment fails; keep every post-connect uncertainty mapped to non-replayable `outcome_unknown` | Fixed in `core_client.py`. `_exchange` distinguishes pre-connect unavailability from post-connect uncertainty, and regression tests prove both the same-id safe retry and the unchanged disconnect-after-send fence. |
| Native neural work outlived the generic client and lane deadlines | Healthy 35-48 second embedding, retrieval, and capture calls became `outcome_unknown` at 15 seconds or poisoned the serialized core after 30 seconds | Give the closed neural operation set one measured bounded deadline on both sides and bind MLX execution to a thread-local stream in every worker | Fixed in `core_client.py`, `core_service.py`, and `mlx_backend.py`: neural calls have a 120-second floor below the 300-second protocol maximum, ordinary RPCs retain the 30-second fence, mutations still never replay automatically, and RPC/capture threads enter the backend execution context. Covered by timeout-floor, lane-health, and execution-context regressions. |
| Verified paired recovery outlived the global five-minute request horizon | Large but healthy signed backup, verification, isolated restore, capture-ledger repair, retention, and replication checkpoint work could return `outcome_unknown` early and then falsely poison the ordinary 30-second backend lane | Add one authenticated closed long-recovery allowlist with a measured bounded hour across protocol, client, service health, and checkpoint surfaces while retaining all ordinary and governance deadlines | Fixed in `core_protocol.py`, `core_client.py`, and `core_service.py`: only the eight recovery operations plus replication create/stage receive 3,600 seconds; ordinary, bridge, pairing, revocation, ACK, evidence, and cutover clocks are unchanged, and serialized-lane acquisition remains capped at 300 seconds. Health identifies recovery maintenance and remaining bounded time, while request-journal reconciliation and no-blind-replay semantics remain intact. Covered by allowlist boundary, client floor, queue bound, five-minute health, route, recovery, and replication regressions. |
| Memory Hygiene loaded one large legacy relationship graph twice during Start Work | Endpoint expansion and rich edge bodies could exceed the response frame, while the sampled backlog looked complete and duplicate scans wasted time | Page the memory list under one authenticated snapshot, exclude graph edges, expose bounded-scan completeness, and reuse the result in Context Health | Fixed in `dashboard_server.py`: 50-entry compact cursor pages advance without duplication, the latest 250 entries are assessed, exact scanned/total counts and snapshot identity are returned, malformed pagination fails closed, and Start Work reuses the same hygiene result. |
| Paired recovery could prove an isolated restore but could not safely promote that restored state into live authority | A valid bundle could still be installed while stale writers or a mismatched request journal survived, breaking exactly-once and recovery lineage | Add quiescent writer inventory, an exclusive authority lease, signed live-state attestation, exact memory/runtime/request-journal binding, explicit restored-target verification, stabilized launchd health, and binding publication only after success | Fixed by the authoritative-core cutover and restored-target protocol in `recovery_manager.py`, `core_service.py`, `scripts/core_cutover_preflight.py`, and `scripts/core_agent_installer.py`; ordinary live stores and promoted restores use distinct fail-closed evidence chains. |
| Foreign-signer recovery review pinned database and capture bytes but not every dependent artifact identity | A self-consistent foreign bundle could preserve reviewed memories while substituting runtime policy, a governed journal, or a receipt after verification | Require an independently supplied SHA-256 for every artifact present and bind the verified bundle/dependent receipt identities through materialization | Fixed end-to-end in recovery manager, database restore sink, core contract, backend, CLI, and MCP surfaces. Missing or mismatched database, capture, governed-journal, or present-runtime pins fail before materialization; receipt/artifact swaps fail before output creation; and a fully pinned foreign governed bundle restores and reverifies its bound journal/runtime state. |
| Loopback dashboard authorization relied on one host-scoped browser credential | A hostile local page, or another listener on a different loopback port, could attempt to reuse ambient browser authority against operator controls | Use an owner-only rotating bootstrap to issue a port-specific HttpOnly SameSite=Strict cookie plus a distinct port-scoped header capability; require both on every API call and exact Host/Origin on POST | Fixed in `dashboard_server.py`, `web/app.js`, `scripts/open_dashboard.py`, the installer, and smoke tests. The auth file is `0600` inside `0700`, contains no cookie secret, and is never replaced by a bare URL launch. The browser keeps `X-Synapse-Dashboard-Session` only in port-scoped `sessionStorage` and scrubs its fragment; installer and smoke probes use the same two-capability contract. |
| Deterministic no-effect delivery rejects accumulated as ambiguous accepted mutations | Invalid ACK/release/dead-letter traffic could exhaust accepted capacity even though the store proved no commit, while broad error reclassification could hide genuinely uncertain outcomes | Reserve a typed no-effect delivery rejection, finalize it as terminal `failed` / `invalid_request`, reject credential-shaped delivery identities before journaling, and keep uncertain failures `outcome_unknown` | Fixed in store, core dispatch, protocol validation, and request-journal tests. Deterministic rejects preserve accepted capacity and deduplicate until age-based pruning; accepted/ambiguous evidence is never evicted, and the total retained-row ceiling deliberately keeps sustained throughput finite. |
| Raw embeddings could resize the dense substrate beyond the reviewed resource envelope | A protocol-valid high-dimensional vector could allocate hundreds of MiB before the journal or operator profile exposed the change | Require exact configured vector dimension before journal admission and require the exact steady float32 topology to fit 384 MiB before MLX load/materialization/resize | Fixed in core and backend admission with adversarial tests. The bound covers sensory/lateral/membrane/spike/active-trace arrays; it does not claim peak process residency, target-hardware counters, or execution time. |
| Runtime JSON and compound SQLite writes could tear or partially commit | Power loss, disk-full, or concurrent writers could leave state unreadable or recall indexes out of sync | Use private same-directory temp files, fsync plus atomic replace, explicit SQLite transactions, integrity audit revisions, and fenced repair | Fixed in `mlx_backend.py` and `memory_store.py`; governed runtime state v3 binds the exact durable marker/epoch/lock generation. SQLite and filesystem publication are separately durable; only the exact pending same-lock-generation crash window described above is recoverable, while unrelated mismatch fails closed. No cross-filesystem atomic transaction is claimed. |
| Context delivery could advance observation state without durable proof of consumption | A reconnecting agent could skip or duplicate work while the UI still looked current | Add leased at-least-once delivery, per-attempt fenced receipts, atomic batch ACK, release/NACK, ACK tombstones, bounded retries, and governed dead-letter audits | Fixed across store/backend/CLI/MCP/dashboard; covered by `tests/test_context_event_delivery.py` and integration tests |
| Compound CLI writes could hide a proven memory commit when the follow-on context notification response was lost | An operator or readiness probe could retry a non-replayable publication, duplicate work, or report the durable primary memory as missing | Give each CLI notification a unique correlation id, reconcile the exact event through bounded read-only inspection, retain the primary receipt when notification state remains unknown, and never replay the ambiguous mutation | Fixed in `synapse_cli.py` and the operator-readiness evaluator. Successful reconciliation returns the exact event; unresolved delivery remains explicit and non-replayable while recall independently proves the primary memory. Atomic conversation capture remains the preferred path when memory plus deployment must share one SQLite transaction. |
| Direct CLI hydration inherited a foreign project or shell working directory when no route was explicit | Real memories could be written to another repository's local `.synapse_s2` store and remain invisible to the authoritative dashboard | Auto-discover and validate the installed owner-only core binding before backend imports; retain explicit `--memory-db`/`--state` only for reviewed offline work | Fixed in `synapse_cli.py` with a foreign-project regression in `tests/test_cli.py`. An installed binding now wins over `CODEX_PROJECT_DIR`, `CLAUDE_PROJECT_DIR`, and cwd for implicit direct-CLI routing; an existing invalid binding fails closed, while hosts with no installed binding retain the legacy v5 bootstrap lane. |
| Capture retry after a crash could create duplicate memories and relationships | The database commit could succeed while receipt cleanup failed, causing the same inbox file to be replayed | Bind a canonical request digest to a unique `capture_id`, commit the capture-operation ledger with memory graph mutations, persist receipts atomically, and reconcile claims after restart | Fixed in `capture_daemon.py`, `memory_store.py`, and `mlx_backend.py`; covered by crash-window and concurrency tests |
| A bounded hot-runtime cutover left historical processed v2 captures with graph/deployment effects but no authoritative ledger rows | Replaying those files could duplicate graph effects, while backing up without ledger authority could preserve an unrecoverable exactly-once gap | Add a read-only exact-evidence audit and a confirmed revision-bound legacy projection that writes only missing ledger rows plus a content-free maintenance receipt after a verified safety backup; never replay graph effects or synthesize transport receipts | Fixed in `recovery_manager.py`, backend/CLI/MCP/readiness surfaces; covered by `tests/test_capture_ledger_reconciliation.py` and recovery integration tests |
| Secret filtering was shallow and retained raw-content digest or diagnostic residue | Nested metadata, exception text, filenames, logs, hashes, or rows written before a detection upgrade could become durable sensitive residue | Recursively redact values, reject credential-shaped identifiers without reflection, strip raw-content digests from fields and free-form strings, use bounded no-follow reads, private files, sanitized public errors, and versioned transactional legacy scrubs | Fixed in `redaction.py`, `memory_store.py`, and every capture/API/CLI/runtime boundary; covered by adversarial redaction, migration, and argument tests |
| SQLite-only copies omitted exactly-once capture transport and had no cryptographic restore proof | A nominally healthy database backup could silently strand replay debt, bind to the wrong capture root, or fail only during an emergency | Create a signed paired database plus capture bundle, bind schema/provenance/reconciliation, reverify all four artifacts, prove an isolated restore, and use signed crash-recoverable reversible retention | Fixed in `memory_store.py`, `recovery_manager.py`, backend/CLI/MCP/dashboard/readiness surfaces; covered by `tests/test_backup_recovery.py` and cross-surface tests |
| Signed bundle verification trusted processed capture ID membership without re-deriving the request binding | A digest-correct, locally re-signed archive could preserve matching IDs/counts while its processed request no longer matched the snapshot ledger | Re-canonicalize processed v2 payloads against protocol/fingerprint/context/source/speaker, derive a content-free binding revision, require it for cutover readiness, and reproduce it after isolated extraction | Fixed in `recovery_manager.py`; covered by signed-artifact mismatch, stable-proof, and isolated-restore tests |
| Historical capture errors accumulated beside active failures | Doctor could not distinguish terminal discard proof from a current raw or retryable failure | Classify artifacts as terminal, historical, unsafe, or unresolved; add content-free preflight plus fenced confirmed archival with crash-recoverable manifests | Fixed in `capture_daemon.py`, CLI, MCP, and Doctor; covered by governance and crash-recovery tests |
| MCP discovery initialized the full dense neural substrate twice | Cold `fastmcp list` and status calls exceeded client timeouts and wasted unified memory | Add a thread-safe control-plane backend and surface-only startup hydration; materialize MLX arrays only for neural operations | Fixed in `mlx_backend.py`, `client_session_bridge.py`, client config, and launcher; cold discovery/status operationally verified |
| Agent-facing memory, graph, hydration, and Cortex responses were unbounded and repeated rich evidence | A single tool result could consume tens of thousands of model tokens, obscure safety fields, duplicate a leased event separately from its receipt, or let prevalidation echo an undeclared secret-bearing argument | Add a canonical versioned compact/full response envelope, exact post-redaction UTF-8 structured-content ceilings, an independently bounded MCP safety-text channel, allowlisted counted omissions, receipt/event one-to-one validation, lease release on projection failure, compact source caps, schema-closed prevalidation that never reflects rejected fields, CLI legacy compatibility, and unchanged rich dashboard APIs | Fixed in `token_contracts.py`, `mcp_server.py`, `synapse_cli.py`, `client_config.py`, and response-contract documentation; covered by contract, MCP, CLI, and client-config tests. Installed compact `structuredContent` is authoritative and capped at 12,288 bytes; its safety text is separately capped at 4,096 bytes; full safety text is separately capped at 131,072 bytes; transport framing is excluded. Contract tools reject undeclared fields before FastMCP validation, including middleware-bypassed internal calls, while publishing `additionalProperties: false`. |
| Recall used a legacy stateful query as its default read path | A nominal read could run the recurrent network, update STDP/runtime state, prune, mark activity, or populate cache state; string output also obscured scope, completeness, and score meaning | Add a structured pure Retrieval v2 path that uses durable spike/surface indexes, optional bounded same-context graph evidence, deterministic weighted fusion, and bounded MMR selection | Fixed in `mlx_backend.py`, `core_service.py`, `core_client.py`, `mcp_server.py`, `synapse_cli.py`, `dashboard_server.py`, and token contracts. MCP `retrieve_spiking_memory_v2` and CLI `retrieve-v2` are the supported read-only recall surfaces; legacy query tools are explicitly deprecated/stateful. Returned items retain stable IDs plus source, graph, and scope/link provenance, and label scores as uncalibrated ranking signals rather than probabilities. |
| Compact memory list, graph, and Cortex reads had no authoritative continuation | A client could not traverse a stable result set, might retry page one, or could mistake a short bounded response for complete state | Add exact totals and authenticated keyset cursors bound to contract/profile/context/scope/filters/order/snapshot/expiry/origin, with independent graph stream positions and a composite Cortex durable-plus-live-runtime snapshot | Fixed in `retrieval_cursor.py`, `memory_store.py`, `mlx_backend.py`, `token_contracts.py`, core/MCP/CLI surfaces, and pagination integration tests. Tampering, expiry, stale content, active-session drift, mismatched context/mode/filter/scope/order, or another origin fails closed and never silently restarts. |
| Retrieval quality and purity had no reproducible regression gate | Ranker changes could leak namespaces, duplicate results, drift across insertion order, mutate state, or improve one metric while degrading another without evidence | Add a fixed labeled multi-namespace corpus, disposable stores, graded relevance metrics, deterministic cross-run comparison, namespace/provenance checks, and physical/logical read-purity digests | Closed by `scripts/measure_retrieval_v2.py`, `tests/fixtures/retrieval_v2/benchmark_v1.json`, and measurement tests. The gate measures Recall@k, MRR, nDCG@k, leakage, duplicate/near-duplicate rates, score/provenance validity, deterministic output, and purity. It is deliberately synthetic and does not claim live-corpus relevance, provider parity, workload capacity, or an SLO. |
| Phase 6 lacked a reproducible acceptance measurement | Unverified reduction percentages could conflate compact source caps with projection savings, use a different fixture, or hide omitted safety evidence | Measure only against an isolated verified recovery restore, bind clean source before and after publication, report installed-policy and same-source comparisons separately, independently verify both output channels, and publish sanitized aggregate evidence | Closed by `scripts/measure_token_contracts.py` and `docs/evidence/phase6-token-contract-acceptance.json`, bound to commit `519af91`. All 11 gates passed. Informational byte reductions are 96.818% installed-policy and 78.03% same-source; token counts and transport framing are excluded. |
| LaunchAgent replacement raced `RunAtLoad` with `kickstart -k` | A healthy old service could be unloaded while the replacement and rollback both failed to settle | Wait for complete launchd unload, let `RunAtLoad` start the new job, use a bounded cold-start health window, and restore exact prior plist/policy on failure | Fixed in both installers; covered by 18 activation, policy, and rollback tests plus live reinstall proof |
| No-memory recall fabricated `context::neuron-*` labels | Could look like historical memory when no memory existed | Return transparent raw activation summary instead of fake tags | Fixed in `mlx_backend.py`; covered by `tests/test_backend.py` |
| Context bus published events without consumption receipts | Could claim "deployed to agents" when no connected client had pulled anything | Add normalized targets, leased at-least-once delivery, fenced attempt receipts, and explicit batch acknowledgement | Fixed in `memory_store.py`, `mlx_backend.py`, `mcp_server.py`, `dashboard_server.py`, `web/app.js` |
| GUI published context events but did not acknowledge its own pulls | Operator could not tell whether the dashboard consumed the event it displayed | Dashboard leases only as `dashboard-ui`, renders the result, then acknowledges the exact returned receipt ids | Fixed in `web/app.js`; covered by dashboard route tests |
| Canned demo-memory path remained available from ops surfaces | Polluted real memory with static content and could undermine trust in recall | Remove production CLI `seed-demo`; default prep to `default` context and factual preflight evidence | Fixed in `synapse_cli.py` and `scripts/prep_tomorrow.sh`; guarded by CLI and operational tests |
| CLI writes were not published to the context bus | CLI-captured thoughts would not appear in agent deployment pulls | Publish CLI remember/ingest writes and add CLI pull/ack cursor commands | Fixed in `synapse_cli.py`; covered by `tests/test_cli.py` |
| Agent/operator conversation notes had no first-class capture path | Future sessions would not naturally appear in the event relationship visualizer | Add `capture-session`, `capture_spiking_conversation`, `/api/capture-conversation`, and GUI capture form | Fixed in backend, MCP, CLI, dashboard, and GUI tests |
| Bad graph data could not be surgically removed | Sensitive, wrong, or partial-truth memory could keep influencing recall | Add confirmed pruning for memory nodes, event nodes, edges, context deployments, temporal edges, and associative edges | Fixed in `memory_store.py`, backend, MCP, CLI, dashboard, and GUI |
| README/runbook still taught `board-demo` and `seed-demo` | IT operators could accidentally present synthetic state | Rewrite examples around `default` and real operator captures | Fixed in `README.md` and `docs/TOMORROW_RUNBOOK.md` |
| Compliance matrix under-described client registration and delivery receipts | Proposal mapping lagged implementation | Add explicit rows for config installer and context cursors | Fixed in `docs/PROPOSAL_COMPLIANCE.md` |
| Capture required an active user-facing tool call | Useful session notes could be missed if the dashboard or agent forgot the synchronous capture form | Add a launchd-backed local capture inbox with CLI, MCP, dashboard status/process controls, redaction, processed/error queues, and tests | Fixed in `capture_daemon.py`, `synapse_cli.py`, `mcp_server.py`, `dashboard_server.py`, `web/app.js`, and `scripts/install_capture_daemon.sh` |
| Restarted agents had to manually compose raw pull, recall, graph, and ack calls | Codex/Claude could miss relevant memory or acknowledge context before actually using it | Add one context-hydration command/tool that returns an agent-ready brief plus leased receipts; require a separate receipt acknowledgement after successful consumption | Fixed in `mlx_backend.py`, `synapse_cli.py`, `mcp_server.py`, `AGENTS.md`, and tests |
| Client startup and shutdown had no repeatable SYNAPSE-S2 habit | Clients could reconnect without hydrating or recording a useful session boundary | Wrap the local MCP launcher with startup hydration and sanitized exit capture; install per-client agent ids | Fixed in `client_session_bridge.py`, `mcp_client_wrapper.py`, `client_config.py`, launcher script, and tests |
| Text projection had no provider boundary or semantic provenance | Recall quality and claims were hard to audit because text embedding looked like fixed lexical hashing | Add local pluggable embedding providers, neural/hash/callable modes, and stored provider metadata | Fixed in `embedding_providers.py`, `mlx_backend.py`, CLI/MCP/dashboard surfaces, and tests |
| No bundled large neural embedding provider was wired into clients | "Semantic memory" could be dismissed as concept hashing instead of model-backed meaning | Add `mlx-neural-v1` using `mlx-community/Qwen3-Embedding-0.6B-4bit-DWQ`, local model cache, CLI/MCP provider benchmarks, and neural-native certification visibility | Fixed in `embedding_providers.py`, `client_config.py`, launcher/prep/capture scripts, `synapse_cli.py`, `mcp_server.py`, docs, and tests |
| Bayesian surprise was lexical-only | Event cuts could be dismissed as word-overlap segmentation instead of semantic boundary detection | Drive event boundaries from the configured local embedding provider, persist semantic and lexical boundary scores, and keep lexical fallback for provider failures | Fixed in `event_segmenter.py`, `mlx_backend.py`, docs, and tests |
| Native execution could not produce a certification evidence payload | IT could challenge whether MLX/mlxsnn/envelope/prune-budget claims were currently true | Add `certify-runtime`, `certify_spiking_runtime`, dashboard certification endpoint/action, strict native checks, and evidence-pack writing | Fixed in backend, CLI, MCP, dashboard, `scripts/prep_tomorrow.sh`, and tests |
| Agents had no real-time cognitive governance loop | SYNAPSE-S2 could remember context but did not actively shape an agent's next move before mutation | Add Cortex Governor enter/tick/commit/close/state APIs with typed truth posture, confidence, evidence, scoped file/tool intent, warnings, MCP/CLI/dashboard controls, moderation controls, and hydration visibility | Fixed in `mlx_backend.py`, `synapse_cli.py`, `mcp_server.py`, `dashboard_server.py`, `web/*`, and tests |
| Already-running local apps had no operator attachment path | Useful app context could remain outside SYNAPSE-S2 unless manually pasted into conversation capture | Add App Connect detection, confirmed app attachment, redacted Accessibility snapshots, selected-text one-shot capture, transcript file delta sources, CLI/MCP/dashboard controls, and operational helper script | Fixed in `transcript_capture.py`, `synapse_cli.py`, `mcp_server.py`, `dashboard_server.py`, `web/*`, `scripts/capture_frontmost_selection.sh`, and tests |
| Capture inbox wrote raw payloads before redaction | Pending files could briefly preserve sensitive text even if ingested memory was cleaned later | Redact text and metadata before disk write, store input hash/redaction count, and keep local files private | Fixed in `redaction.py` and `capture_daemon.py`; covered by capture daemon tests |
| Capture inbox could follow symlink payloads | A local symlink in the inbox could redirect processing into an unintended file | Reject symlink and non-regular pending payloads before reading | Fixed in `capture_daemon.py`; covered by symlink rejection tests |
| Direct capture responses could return raw namespace payloads after sanitized persistence | Operators or MCP clients could see secrets in response/deployment JSON even though SQLite rows were redacted | Build namespace response nodes from stored sanitized entries and run context-bus payloads through the shared redactor | Fixed in `mlx_backend.py`; covered by backend and MCP redaction regression tests |
| Manual capture processing and dashboard App Connect used programmatic self-confirmation | A stale click or client call could process pending files, attach an app, or snapshot a connection without a current operator-visible target review | Add capture-inbox preflight summaries, dashboard one-time confirmation tokens bound to the target file/app/connection, and require CLI/MCP manual inbox confirmation | Fixed in `capture_daemon.py`, `dashboard_server.py`, `mcp_server.py`, `synapse_cli.py`, and `web/app.js`; covered by dashboard/MCP/CLI tests |
| MCP memory pruning did not require an explicit confirm parameter | A client could accidentally issue a destructive prune call | Require `confirm=true` for MCP pruning, matching CLI/dashboard confirmation semantics | Fixed in `mcp_server.py`; covered by MCP tests |
| Cortex trace pruning bypassed the explicit confirm contract | A governed trace could be deleted through MCP/CLI/dashboard moderation without the same destructive-action guard as graph pruning | Add `confirm` gating to backend, MCP, CLI, dashboard API, and browser request path for Cortex prune | Fixed in `mlx_backend.py`, `mcp_server.py`, `synapse_cli.py`, `dashboard_server.py`, `web/app.js`; covered by backend/MCP/CLI/dashboard tests |
| Cortex dashboard defaulted casual commits to high-confidence `test-validated` validation | Operators could accidentally turn an unverified note into a high-confidence future memory | Default UI commits to observed evidence and require concrete validation evidence before accepting `test-validated` | Fixed in `web/index.html` and `mlx_backend.py`; covered by backend/dashboard tests |
| Dashboard could be bound to non-loopback hosts too casually | The local operator control plane could become reachable on a LAN by mistake | Refuse every non-loopback bind and validate the HTTP Host header against the configured loopback endpoint | Fixed in `dashboard_server.py`; covered by dashboard tests |
| Existing malformed client config could be overwritten silently | Recovery from a bad config could hide operator state or erase manual context | Refuse malformed existing JSON and require explicit operator repair | Fixed in `client_config.py`; covered by client config tests |
| Prep script had no low-mutation audit path | IT reviewers had to run the full mutating preflight to get confidence | Add `scripts/prep_tomorrow.sh --verify-only` for tests, compile, status, profile, certification, and preflight without installs, captures, MCP wrapper launches, dashboard smoke, maintenance, or backups | Fixed in `scripts/prep_tomorrow.sh`; covered by operational script tests |
| Spike-overlap recall scanned every memory row | Query latency could scale linearly as durable memory grew | Add a durable `memory_spikes` inverted index, migrate existing rows, update it atomically on upsert, and query overlap through indexed SQLite joins | Fixed in `memory_store.py`; covered by memory-store and backend recall tests |
| Surface-text recall rebuilt a large Python index after memory changes | First recall after capture could pull and tokenize thousands of full memory rows | Add durable `memory_surface_terms` token/facet index, migrate/backfill existing rows, update it atomically on upsert, and query prompt terms through indexed SQLite joins | Fixed in `memory_store.py` and `mlx_backend.py`; covered by memory-store/backend tests |
| Graph serialization decorated relationship endpoints one row at a time | The relationship visualizer could show edge rows whose endpoints were not present in the drawn node set and could perform N+1 entry reads | Batch-load relationship endpoints and prioritize them before recent noise in `list_memory_graph` | Fixed in `memory_store.py` and `mlx_backend.py`; covered by backend graph tests |
| Dashboard used a single unbounded request lane | A slow local graph, certification, or evidence-pack request could monopolize the UI, while unconstrained threading would create a new exhaustion path | Bound threaded handling to eight active handlers/backlog 32, absolute one-second complete-header admission, five-second post-header I/O, and bounded shutdown | Fixed in `dashboard_server.py`; covered by concurrency, slow-header, timeout, saturation, and shutdown tests |
| Operators had no first-screen daily workflow | New users could ask "what do I do next?" even when the underlying MCP tools worked | Add Start Work, Context Health, Doctor/Repair, Memory Hygiene, Wrap Session, recipes, and operation receipts across CLI/dashboard | Fixed in `dashboard_server.py`, `synapse_cli.py`, `web/*`, and tests |
| Active goals were inferred from sessions but not available as a durable operator ledger | Operators could lose "what are we doing and why?" across days unless the task was embedded in a Cortex session note | Add governed Goal Ledger traces with create/update/list commands, MCP tools, dashboard rendering, and Start Work morning brief integration | Fixed in `mlx_backend.py`, `synapse_cli.py`, `mcp_server.py`, `web/*`, `README.md`, `docs/TOMORROW_RUNBOOK.md`, and tests |
| App Connect could write a low-signal snapshot without a preview receipt | Operators could attach an app and still not know whether the snapshot contained useful context | Add `app-snapshot-preview`, dashboard quality/capability badges, no-write blocked receipts, signal/redaction counts, and selected-text fallback guidance | Fixed in `transcript_capture.py`, `dashboard_server.py`, `synapse_cli.py`, `web/*`, and tests |
| Recall results were visible but not easy to bless for current work | Useful recalled context could stay transient instead of becoming explicit operator-confirmed evidence | Add Recall Pin with typed Cortex trace commit and operation receipt | Fixed in `dashboard_server.py`, `web/app.js`, and dashboard tests |
| Dashboard context selector was manual-only | Operators had to remember exact namespace names such as `default`, `servus-hydrated-handoff-20260630`, or coworker handoff contexts, increasing typo and wrong-context risk | Populate the dashboard Memory Context control from live `status.memory_contexts`, keep `default` first, update the URL/input/current memory URI on selection, and preserve manual entry for new namespaces | Fixed in `web/index.html`, `web/app.js`, `web/styles.css`, and dashboard tests |
| No committed point-in-time status report | Feature, technology-stack, current-status, and non-claim documentation could drift from live runtime reality | Add `scripts/synapse_status_report.py` to collect live status/profile/Doctor/context-health/hygiene/Cortex state and write `docs/CURRENT_STATUS.md` before demos, handoffs, and readiness claims | Fixed in `scripts/synapse_status_report.py`, `docs/CURRENT_STATUS.md`, `README.md`, compliance docs, and tests |
| Closed Cortex sessions could be resurrected by a stale long-running process | A dashboard or capture daemon that loaded old runtime state could later write the whole state file and turn already-closed sessions active again | Merge existing `cortex_sessions` from disk before every runtime-state write, and make terminal session states win over stale active copies | Fixed in `mlx_backend.py`; covered by `tests/test_backend.py::test_cortex_close_survives_stale_backend_runtime_persist` |
| A killed MCP wrapper could leave its Cortex session active indefinitely | The dashboard could show phantom governed work after a short-lived client crashed or was terminated before its exit handoff | After capture work, track the exact MCP ownership tuple ephemerally and reap only after two consecutive definite-missing probes plus the 15-second client deadline; clear candidates on live/unknown probes, ownership changes, or terminal state, preserve late valid finishes, and publish only content-free maintenance health | Fixed in `core_service.py` and `mlx_backend.py`; covered by focused filtering, strict process-probe, grace, ownership-change, pre-authority activation, persistence rollback/reload, heartbeat, ordering, and error-recovery tests |

## Remaining Explicit Non-Claims

| Gap | Risk | Shorthand solution | Current disposition |
| :--- | :--- | :--- | :--- |
| Multi-Mac replication is an offline whole-checkpoint recovery protocol, not live merge or federated recall | Operators could expect a PTZPLZ namespace created on one Mac to automatically discover or bridge a CASP namespace that exists only on another Mac | Co-locate reviewed context before governed bridging today; design a signed remote catalog plus governed transfer or read-only federated retrieval before claiming live cross-Mac memory | Explicitly not claimed. Pairing, checkpoint creation, staged receive, and promotion preserve one authoritative store for recovery; they do not merge two live databases or automatically create cross-host namespace links. |
| No guaranteed invisible interception of arbitrary private transcript stores | Running clients must still call a capture tool, write a local inbox payload, expose a transcript/log file, provide selected text, or be attached through App Connect for full chat text to enter memory | Use the MCP startup/session-boundary bridge for process boundaries, App Connect for visible running apps, transcript sources for local logs, and selected-text capture for apps that block snapshots | Documented limitation; bridge, inbox, App Connect, transcript deltas, selected-text capture, durable capture, and pull/ack work now |
| Bayesian surprise is deterministic provider-distance segmentation, not online probabilistic token-stream inference | Event cuts are semantically provider-backed and auditable, but they are not a full Bayesian posterior over live token probabilities | Add optional online probabilistic calibration if future clients expose token-level streams | Research extension, not needed for current MCP stdio reliability |
| Resource envelope certification is MLX/topology evidence, not Instruments counters | Hardware-level memory certification is not yet captured from Apple Instruments traces | Add Instruments/Metal counter harness across target Apple Silicon SKUs | Research extension, not claimed complete |
| Authorized filesystem paths are not yet consumed fd-relative end to end | A malicious process already running as the same macOS user with comparable directory write access could race a checked path after final authorization but before a legacy SQLite/archive/capture consumer reopens it | Extend the path-capability contract through fd-relative SQLite, archive, retention, and capture APIs so no downstream component reopens a pathname | Explicitly not claimed. Current controls pin and revalidate no-follow descriptors, enforce owner-only files/directories, serialize mutations, verify digests/receipts, and assume the owner account is trusted; do not expose the socket to mutually untrusted same-UID clients. |
| Dense lateral matrix limits very large neuron counts | Higher topology sizes can exceed local memory quickly | Add sparse/block lateral matrix backend | Research extension, not claimed complete |
| Very large graph visualization remains a bounded local dashboard view | The backend now batches endpoint hydration, but the browser still renders a bounded graph rather than a streaming graph database UI | Add paginated graph exploration, server-side layouts, or search-driven subgraph expansion if memory grows into tens of thousands of nodes | Product extension, not needed for tomorrow-ready local operations |
| Retrieval snapshot generations are enforced by temporary triggers on governed writer connections | A same-UID process that violates the authoritative-core contract and writes SQLite directly could bypass those connection-local triggers, leaving a cursor revision unchanged after out-of-contract mutation | Keep the database private behind the authoritative core, reject legacy writers, audit counter integrity, and move the triggers into a future durable schema revision only if external governed writers become a supported deployment model | The production path no longer hashes every memory body per page. Normal and maintenance/repair write connections cover memory, relationship, Cortex, spike-index, and surface-index mutations; the revision hashes transaction-coupled per-namespace generations, exact counts, and the semantic-index generation. Missing derived tables remain repairable because their triggers are installed immediately after reconstruction. Malformed or overflowing counters fail closed. Direct raw SQLite mutation remains explicitly unsupported. |
| Ranked Retrieval v2 is bounded but not cursor-paginated | `completeness.has_more` can indicate candidate or result truncation, but there is no stable continuation through the relevance-ranked set | Add a separately versioned ranked-search continuation only if a real operator workflow requires it; it must bind the query fingerprint, ranker/embedding identities, scope snapshot, candidate bounds, and stable tie-breakers | Explicit by design: `pagination_supported: false` and `next_cursor: null` on `memory-retrieval`. Callers may issue a larger bounded request or begin a new snapshot; they must not treat `has_more` as a cursor. |
| Retrieval v2 validation uses a fixed synthetic fixture | A passing benchmark can miss live vocabulary, namespace imbalance, embedding-provider differences, concurrency, drift, and operator relevance judgments | Add privacy-preserving live shadow evaluation and human relevance labels before making quality or SLO claims | Synthetic regression gate only; `docs/evidence/phase8-retrieval-v2-acceptance.json` is bound to clean implementation commit `738cfce` and passes the fixture, determinism, scope, provenance, and read-purity gates. The harness intentionally never opens the live memory database and its latency samples are informational. |
| Future-dated S2-Net/SDA/SGTN citations were presented as implementation inspiration | Readers could infer that SYNAPSE-S2 implements or validates published biological synchronization algorithms | Treat the citations as unverified proposal context and describe only the algorithms actually present in code | Explicit non-claim: no S2-Net phase-delay engine, SDA spike-train attention operator, or SGTN model is implemented or validated. Namespace links, galaxy styling, and Retrieval v2 are deterministic operator-governed product mechanisms. |
| Strict native mode requires explicit enablement | Default developer mode still permits fallback so non-native hosts can run tests and local tooling | Set `SYNAPSE_S2_REQUIRE_NATIVE=1`, CLI `--require-native-backend`, preflight `--require-native`, or run certification with `strict_native=true` | Hard-fail and certification path implemented |

## Current Production Bar

The current bar for calling a local build presentable is:

1. A clean source revision passes `scripts/prep_tomorrow.sh --verify-only`; a production apply additionally uses a fresh readiness manifest plus reviewed binding and passes `scripts/prep_tomorrow.sh --apply --install-core /absolute/path/to/manifest.json`.
2. `get_spiking_attention_status` reports runtime ready, enabled, and shared `.synapse_s2` paths.
3. `pull_spiking_context_deployments` leases the oldest eligible durable events and returns fenced receipt ids without advancing acknowledgement state.
4. `ack_spiking_context_deployments` atomically acknowledges the exact consumed receipt ids; the durable-disposition cursor advances only across contiguous acknowledged or governed terminal events.
5. Retry attempts stop at `SYNAPSE_S2_CONTEXT_MAX_DELIVERY_ATTEMPTS`; an exhausted event remains visibly blocked until an operator uses the confirmed, reason-bearing dead-letter action, which writes a governance audit before later events advance.
6. No-memory recall returns a transparent raw activation summary, never a fake historical tag.
7. Conversation capture creates visible event nodes in the graph and a durable context-bus deployment.
8. Confirmed pruning can remove a single node, edge, deployment event, temporal edge set, or associative edge set.
9. The capture inbox sidecar is installed or confirmed `capture-inbox-process --confirm` / `process_spiking_capture_inbox(confirm=true)` proves pending drops become graph events with secret redaction.
10. Redaction regression tests prove raw secret shapes do not survive pending inbox files, direct capture memory, context-bus deployments, or returned MCP/API payloads.
11. Exactly-once capture replay, receipt-write failure, concurrent duplicate, and restart reconciliation tests pass; capture status reports no unresolved or unsafe artifact.
12. MCP prune calls fail without `confirm=true`, CLI prune calls require `--confirm`, dashboard destructive graph actions require explicit confirmation, Cortex trace prune follows the same confirm contract on all surfaces, and dashboard App Connect/Magic Capture writes require preflight confirmation tokens.
13. The dashboard refuses every non-loopback bind; every API GET/POST requires its port-specific HttpOnly SameSite=Strict cookie plus `X-Synapse-Dashboard-Session`, and POST also rejects an inexact Host or Origin.
14. `scripts/prep_tomorrow.sh --verify-only` exits zero before the full mutating preflight is run.
15. The local MCP launcher enters through the compact control-plane startup bridge, client configs declare distinct agent ids, and cold tool discovery stays inside the client timeout.
16. Text memories show `embedding_provider` provenance from the active local provider, event memories show `surprise_model`, `surprise_mode`, `semantic_surprise_score`, and `lexical_surprise_score`, and `provider-benchmark`/`benchmark_spiking_embedding_provider` reports `mlx-neural-v1` plus `native_mlx: true` for installed client defaults.
17. `certify-runtime` or `certify_spiking_runtime` produces a native evidence payload and reports strict-native failures instead of silently downgrading.
18. `enter-cortex`, `cortex-tick`, `commit-cortex`, `close-cortex`, and `cortex-state` or their MCP equivalents can start a governed session, warn on missing scoped file/tool intent for mutations, warn before risky actions, persist typed validation/decision/constraint/risk traces, and end the session without leaving stale active state.
19. App Connect can list local apps, attach a confirmed app, capture selected text or a redacted app snapshot, and persist those captures as real temporal graph memory.
20. `.venv/bin/python scripts/open_dashboard.py` opens the authenticated dashboard through the owner-only rotating bootstrap; a bare loopback URL is never treated as an operator launch path. The authenticated surface can write, capture conversations, attach and snapshot local apps, process magic capture drops, ingest, enter/tick/commit/close Cortex Governor sessions with scoped intended files/tools, promote/demote/prune governed traces with confirmation for deletion, recall, graph, prune, certify native runtime, sleep, back up, and show context-bus receipt state.
21. Store stats show populated durable spike and surface-term recall indexes, and graph responses prioritize relationship endpoints before recent unrelated nodes.
22. Dashboard HTTP serving is bounded to eight active handlers and backlog 32, complete headers must arrive within an absolute one-second pre-authentication deadline, post-header I/O uses a five-second timeout, and shutdown is bounded.
23. The Daily Operator Trust Loop can produce a Start Work brief, Context Health score, Doctor/Repair report, Memory Hygiene queue, Wrap Session preview/commit receipt, and matching CLI receipts.
24. Goal Ledger traces can be created, updated, listed, surfaced in Cortex state, and included in the Start Work morning brief across CLI, MCP, and dashboard surfaces.
25. App Connect preview returns a quality badge and no-write receipt before a snapshot is committed; blocked or low-signal previews direct the operator to selected-text capture.
26. A recalled item can be pinned into Cortex as `operator-confirmed` evidence with a receipt.
27. `scripts/operator_readiness_certify.py` produces one evidence pack proving client config, MCP connect, the required compact `mcp_contract_probe`, native neural embedding, Doctor, Start Work, memory write, recall, App Connect no-write preview, Wrap Session persistence, and dashboard smoke before claiming SYNAPSE-S2 is operator-ready. The probe independently verifies the installed 12,288-byte authoritative structured envelope and its separate 4,096-byte compact safety text; transport framing is excluded.
28. The dashboard Memory Context control lists existing namespaces from `memory_contexts`, selecting one updates the URL/input/current memory URI/status snapshot, and manual entry remains available for new namespaces.
29. `docs/CURRENT_STATUS.md` is regenerated with `scripts/synapse_status_report.py --context default --embedding-provider mlx-neural` before demos, handoffs, and readiness claims.
30. Closing a Cortex session remains durable across fresh CLI/MCP processes and stale long-running dashboard/capture-daemon writers.
31. Contracted MCP and CLI memory, graph, hydration, and Cortex reads emit `synapse-s2.token-contract.v1` compact envelopes by default, keep authoritative `structuredContent` within its configured post-redaction UTF-8 budget, preserve receipt/event one-to-one delivery safety, state completeness and omissions explicitly, and never reduce the dashboard's rich graph API. MCP safety `TextContent` is verified against its own ceiling and never treated as the authoritative payload.
32. `docs/evidence/phase6-token-contract-acceptance.json` passes all 11 isolated measurement gates, binds the clean source revision, verifies both MCP output channels, and labels installed-policy source-cap-plus-projection reduction separately from same-source projection reduction.
33. Invalid no-effect ACK/release/dead-letter requests become terminal `failed` / `invalid_request` rows, credential-shaped delivery identifiers never enter the journal, and uncertain failures remain non-replayable `outcome_unknown`; total retained-row capacity is still finite until terminal rows age out.
34. Raw query/register embeddings are rejected before journaling unless their dimension exactly matches the bound configuration, and the exact steady float32 topology must fit 384 MiB before MLX loading or materialization. This is not peak-residency, hardware, or timing certification.
35. Foreign recovery requires every present artifact pin, binds the verified bundle and dependent receipt identities through materialization, rejects receipt/artifact swaps before output creation, and proves a fully pinned foreign governed restore with its journal/runtime binding intact.
36. CLI `retrieve-v2`, MCP `retrieve_spiking_memory_v2`, the dashboard query route, and internal hydration recall return the same structured read-only Retrieval v2 semantics: deterministic hybrid ranking, explicit scope/link/source provenance, uncalibrated scores, and no recurrent/STDP/pruning/runtime-state mutation.
37. Compact/full memory list, memory graph, and Cortex state pages report exact totals and `authenticated-keyset-v2` continuation metadata; the next page fails closed if its cursor is altered, expired, stale, used with different bindings, moved across origins, or—in Cortex—if the frozen active-session view changes.
38. `scripts/measure_retrieval_v2.py` must pass its fixed synthetic acceptance fixture without namespace leakage, duplicate IDs/content, provenance/score violations, output nondeterminism, or read-state mutation. The result remains explicitly synthetic and is not reported as live relevance or latency proof.
